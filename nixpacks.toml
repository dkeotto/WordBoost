[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "cd ydt-kelime-pratigi && npm install"]

[phases.build]
cmds = ["cd ydt-kelime-pratigi && npm run build"]

[start]
cmd = "npm start"
